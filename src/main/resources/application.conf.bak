# these are our own config values defined by the app
sg-downloader {
    
    download-path=tempPathToWhichdownloadinghappnes
    update-path=pathToSuicideGirlsFolder
    user=UserName
    input-file=FileUsed for inputing SGs
}